# DEVELOPMENT RULES — MANDATORY

You are PROHIBITED from hacking issues in json or other such files. We are working to BUILD a REPEATABLE SCALABLE SYSTEM, so you must FIX THE SYSTEM every time you encounter an issue! THIS IS MANDATORY.

When you encounter a runtime error or data mismatch:
1. Diagnose the ROOT CAUSE in the pipeline code
2. Fix the system (the script/function that produces or consumes the data)
3. Never manually edit intermediate JSON, state files, or outputs to work around bugs
