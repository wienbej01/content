# S9-C08 — Eliminate module-level PRODUCTION_DB_PATH override (test-isolation defect)

## Metadata
- **Ticket ID:** S9-C08
- **Title:** Remove the module-level `PRODUCTION_DB_PATH` env override in `test_produce_db_orchestrator.py`; scope DB env to a fixture like every other test
- **Requirement / risk IDs:** R8 / D-017
- **Priority / severity:** P2 / MEDIUM (test reliability — undermines the suite the sprint validates against)
- **Risk level:** low–medium
- **Execution class:** COMPLEX
- **Dependencies:** none
- **Blocks:** none
- **Wave:** 1
- **Status:** implemented + full-suite green (2026-06-19); awaits independent validator

## Implementation outcome (2026-06-19)
- **Root cause (CORRECTED from the ticket hypothesis):** the ticket guessed the module-level
  `PRODUCTION_DB_PATH` override. Investigation (AST/instrumentation) showed that override was
  **vestigial** (removing it left the orchestrator tests 9/9 green) and **not** the e2e-breaker.
  The actual leak: `test_produce_db_orchestrator` mutates the module global
  `produce_db.STAGE_INVOKERS` to mock stages, and several tests set mocks **outside** their
  `finally`-restored dict — so 9 `MagicMock` invokers (incl. `audio_timing`, `tts`,
  `compile_media`) leaked into later tests. The `s8_resume` e2e then ran a no-op `audio_timing`
  → no timeline_spans → `reconcile_timing` raised "No active timeline spans". Order-dependent
  (orchestrator→e2e failed; e2e→orchestrator and e2e-alone passed). Confirmed via a probe test
  listing the leaked MagicMocks, and by instrumenting `reconcile_storyboard_with_timing`
  (showed the correct isolated DB but `all_spans=[]`).
- **Fix (two parts):**
  1. `test_produce_db_orchestrator.py` `setup_test_db` autouse fixture now **snapshots
     `produce_db.STAGE_INVOKERS` before each test and restores it after** — catches every mock
     leak regardless of which test/finally. (Also removed the vestigial module-level
     `os.environ["PRODUCTION_DB_PATH"]`.)
  2. New `tests/test_s9_c08_test_isolation.py` — an AST **meta-guard** that fails if any test
     module sets `PRODUCTION_DB_PATH`/`CLIP_DB_PATH` at module scope. Verified it detects the
     old pattern.
- **Validation:** orchestrator 9/9; **both batch orders pass** (orchestrator→e2e and
  e2e→orchestrator, 10/10 each — previously orchestrator→e2e failed); meta-guard passes;
  **full suite 1060 passed, exit 0, 679 s.** Not committed yet.
- **No production code changed** (test-only). The remaining `open_test_reliability_tickets`
  entry for D-017 is now resolved.

## Observable outcome
The full suite is order-independent: every test passes regardless of collection order, and no test leaves a process-global `PRODUCTION_DB_PATH` value that can change another test's database. Concretely, running `tests/test_produce_db_orchestrator.py` immediately before `tests/e2e/test_s8_projections_resume.py` (the order that currently fails) passes, and the full suite stays green. Observable by: a regression test / CI order check that runs the polluting combination and asserts pass; a grep/assertion that no test module sets `PRODUCTION_DB_PATH` at module import scope.

## Evidence and rationale
- **Observed current behavior:** the `s8_resume` e2e test fails with `Stage 'reconcile_timing' failed: No active timeline spans for production …` **only when run after `test_produce_db_orchestrator`** in the same session. It **passes in isolation** (verified: 1 passed, ~36 s, both at clean baseline `3f1c9fa` and with S9-C01 applied). The failure is therefore a cross-test isolation break, not a production-code defect.
- **Supporting paths / the anomaly:**
  - `tests/test_produce_db_orchestrator.py:16-19` —
    ```
    TEST_DB = ROOT / "db" / "test_produce_db.db"
    os.environ["PRODUCTION_DB_PATH"] = str(TEST_DB)   # MODULE-LEVEL — no teardown
    ```
    This is the **only** module-level (top-level) `PRODUCTION_DB_PATH` assignment in the suite. `grep -rn 'os.environ\["PRODUCTION_DB_PATH"\]' tests/` shows **every other** occurrence (test_repair_routing_lb603, test_sprint5_tts_service, test_sprint8_publish, test_sprint2_production_repo, test_diagnostic_audio_lb401, test_sprint7_assemble_db, test_sprint4_authoring, test_sprint6_media_service, test_sprint3_stage_runner, e2e/test_crash_matrix, test_sprint9_migrate) is **inside an autouse fixture paired with a `del os.environ[...]` teardown**.
  - `tests/conftest.py:28-40` — the canonical isolation pattern: autouse `_isolate_production_db` uses `monkeypatch.setenv` (auto-restored) **plus** `production_db._db_path_override` (reset to `None` on teardown). A module-level `os.environ[...] = …` with no teardown is inconsistent with this and is the prime suspect for leaking DB identity across tests.
- **Confirmed:** the failure reproduces at clean baseline (`3f1c9fa`, S9-C01 changes stashed) in the same batch order — so it is **pre-existing and independent of S9-C01**.
- **Root-cause caveat (IMPORTANT):** the isolation mechanism is subtle (conftest monkeypatches per test, so the leaked env *should* be reset per test). The module-level override is the **evidenced prime suspect** but is **not yet proven** to be the sole cause — another `test_produce_db_orchestrator` side-effect (e.g. a shared real-FS `db/test_produce_db.db` or a `_db_path_override` interaction) could contribute. **Reproduce and isolate the actual cause before fixing** (see steps).
- **Why necessary:** the sprint's validation rests on a trustworthy suite; an order-dependent failure both masks real regressions and can produce false failures. The user requires real, reliable tests (no leakage).

## Context capsule
- **Paths:** `tests/test_produce_db_orchestrator.py` (lines 14-19 module-level set; 26-37 `setup_test_db` fixture), `tests/conftest.py` (`_isolate_clip_db`, `_isolate_production_db`), `tests/e2e/test_s8_projections_resume.py` + `tests/e2e/s8_helpers.py` (the failing downstream test), `scripts/production_db.py` (`connect`, `_db_path_override`, `ensure_production`).
- **Key symbols:** `PRODUCTION_DB_PATH`, `production_db._db_path_override`, `monkeypatch.setenv`, `_db.connect(None)`.
- **Invariant:** every test must run identically in any collection order; DB identity is per-test only. Do NOT introduce test-specific production behavior; the fix is in test/conftest wiring only.
- **Convention:** match the existing per-fixture `setenv`+`del` pattern (or rely on conftest's autouse isolation + explicit `db_path=` args).

## Scope
- **Expected change areas:** `tests/test_produce_db_orchestrator.py` (remove the module-level `os.environ` set; if any code path in those tests genuinely needs the env var, move it into the existing `setup_test_db` autouse fixture with a matching teardown, or pass `db_path=` explicitly as the tests already do). Possibly a guard in `conftest.py` or a meta-test asserting no module sets the env at import scope.
- **Permitted inspection areas:** `tests/conftest.py`, all `tests/test_sprint*_*.py` for the fixture pattern, `scripts/production_db.py` `connect`/override resolution, `scripts/produce_db.py` (how `run_production`/invokers resolve `db_path=None`).
- **Protected:** the orchestrator tests' actual coverage (must remain green); conftest's existing isolation; the D-013/D-015 etc. behavior.
- **Exclusions:** do not change production DB-resolution semantics (that is a separate question); do not touch the real run DB.
- **Discovery (confirm during reproduction):** (a) Does `_db.connect(None)` consult `_db_path_override`, `PRODUCTION_DB_PATH`, or both — and in what precedence? (b) After moving the override into a fixture, does the batch pass? (c) Are there other latent cross-test leaks (e.g. real-FS `Videos/Projects/<slug>` or `db/test_produce_db.db` not cleaned)? Record findings.

## Preconditions and baseline
- **Accepted dependencies:** none.
- **Environment:** `YT_TEST_MODE=1`; `ffmpeg`/`ffprobe` (the e2e builds a real audio fixture).
- **Baseline commands:**
  ```bash
  cd /home/jacobw/YTchannel
  git log --oneline -1
  # Reproduce the order-dependent failure (currently FAILS):
  YT_TEST_MODE=1 python3 -m pytest tests/test_produce_db_orchestrator.py tests/e2e/test_s8_projections_resume.py -q
  # Contrast: e2e alone PASSES:
  YT_TEST_MODE=1 python3 -m pytest tests/e2e/test_s8_projections_resume.py -q
  grep -rn 'os.environ\["PRODUCTION_DB_PATH"\]' tests/
  ```
- **Expected current result:** the two-file batch fails at reconcile_timing; e2e alone passes.
- **Blocker conditions:** baseline full suite not green; the batch failure cannot be reproduced (then close as non-reproducible with evidence).

## Implementation steps
1. **Reproduce** (baseline command above) — confirm the batch fails and the e2e alone passes. Capture the exact failure.
2. **Isolate the cause.** Deterministically confirm the module-level override is the trigger: e.g. (a) move only that line into the `setup_test_db` fixture (setenv on enter, `del`/monkeypatch on teardown) and re-run the batch; or (b) temporarily remove the line and re-run. If the batch passes, cause confirmed. If it still fails, continue isolating (shared `db/test_produce_db.db` file, `_db_path_override`, real-FS project dir) until the true root cause is found — record it.
3. **Fix at root.** Make `test_produce_db_orchestrator.py` follow the suite's isolation pattern: no module-level DB env; use the autouse fixture (monkeypatch) or explicit `db_path=` (the tests already pass `db_path=TEST_DB` everywhere). Clean up the real-FS `db/test_produce_db.db` and any `Videos/Projects/<slug>` in teardown if that is part of the cause.
4. **Guard against regressions.** Add a small meta-test (or assert in conftest) that no test module sets `os.environ["PRODUCTION_DB_PATH"]` at module scope — so this cannot silently return. (A `grep`-based collection-time check is acceptable; keep it real, not a mock.)
5. **Verify order-independence.** Run the previously-failing batch and a reversed order; both pass.

## Test and proof matrix
| Level | Scenario | Expected result | Exact command |
|---|---|---|---|
| Regression | orchestrator THEN e2e (the failing order) | passes | `YT_TEST_MODE=1 python3 -m pytest tests/test_produce_db_orchestrator.py tests/e2e/test_s8_projections_resume.py -q` |
| Regression | e2e THEN orchestrator (reversed) | passes | `YT_TEST_MODE=1 python3 -m pytest tests/e2e/test_s8_projections_resume.py tests/test_produce_db_orchestrator.py -q` |
| Unit/meta | no test module sets PRODUCTION_DB_PATH at module scope | assertion holds | `YT_TEST_MODE=1 python3 -m pytest tests/test_s9_c08_test_isolation.py -q` (or equivalent meta-check) |
| Regression | orchestrator tests still pass | green | `YT_TEST_MODE=1 python3 -m pytest tests/test_produce_db_orchestrator.py -q` |
| Regression | full suite, default order | green | `YT_TEST_MODE=1 python3 -m pytest -q` |

## Acceptance gates (binary, observable)
- The previously-failing two-file batch (orchestrator → e2e) passes; reversed order also passes.
- No test module sets `PRODUCTION_DB_PATH` at module-import scope (enforced by a real check).
- Orchestrator tests remain green; full suite green in default order.
- No production code changed (test/conftest wiring only).

## Audit focus
- Was the root cause actually isolated (not just papered over by reordering)? Does the fix preserve the orchestrator tests' intent (they exercise `run_production`/resume on a real ledger)? Could the real-FS `db/test_produce_db.db` or `Videos/Projects/<slug>` still leak? Does the meta-guard run in CI (not just locally)? Did any change weaken conftest's autouse isolation?

## Rollback and recovery
- Test/conftest-only change; `git revert`. No migration, no production behavior change. Re-running the suite is the recovery check.

## Completion evidence
- Files changed; the confirmed root cause (with the isolation experiment that proved it); diff summary; commands + exit codes; tests added (incl. the order-regression + meta-guard); observed results; residual risks; commit if permitted. Append to `EXECUTION_LOG.jsonl`; update `STATE.json`.
