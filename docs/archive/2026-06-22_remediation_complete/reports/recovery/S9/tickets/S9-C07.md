# S9-C07 — Assembly richness: graphics overlay + music bed

## Metadata
- **Ticket ID:** S9-C07
- **Title:** Add a graphics layer and a music bed to `build_assembly_manifest` / `assemble.py`
- **Requirement / risk IDs:** R6 / D-ASM
- **Priority / severity:** P2 / MEDIUM
- **Risk level:** medium
- **Execution class:** COMPLEX
- **Dependencies:** S9-C04 (graphic beats must exist to have graphic content)
- **Blocks:** none
- **Wave:** 3
- **Status:** pending

## Observable outcome
The assembled deliverable includes (a) **graphics** rendered for graphic beats (deterministic text/layout, composited over the visual bed) and (b) a **music bed** mixed under the master narration (ducked so narration stays intelligible), in addition to the clip bed + master narration. Observable by: a contract test asserting `build_assembly_manifest` emits a graphic layer per graphic beat and a music track reference; an integration test (ffmpeg) asserting the assembled output has an audio stream containing the master narration and a music component, and a graphic frame is present for a graphic beat.

## Evidence and rationale
- **Observed current behavior:** `build_assembly_manifest` emits only clip segments + `continuous_audio` (master narration). No graphics or music layer.
- **Supporting paths:**
  - `scripts/assemble_db.py:120-183` — `build_assembly_manifest`: segments from clips; `narration_mode="continuous_voiceover"`; `continuous_audio` = master; no graphic/music keys.
  - `scripts/assemble.py:52` — already imports `generate_music` (`gen_music`, `write_wav`) → a music generator exists.
  - `scripts/assemble.py:253-260,378-385` — existing overlay support reads `assets/overlays/{beat_id}_overlay.png` when `seg["overlay"]["required"]`; `_composite_overlay` composites a PNG over a clip.
  - `scripts/assemble.py:188-297` — continuous-voiceover handling; filtergraph construction.
- **Confirmed root cause:** the manifest never emits graphic/music layers, so the assembler never composites graphics or mixes music.
- **Why necessary:** the user explicitly wants both graphics and a music bed.

## Context capsule
- **Paths:** `scripts/assemble_db.py` (`build_assembly_manifest`, `build_assembly_inputs`), `scripts/assemble.py` (filtergraph, overlay, music import), `scripts/generate_music.py` (`generate`, `write_wav`), `docs/channel_universe/constraints.json` (text/audio policy, crop safety).
- **Key symbols:** `build_assembly_manifest`, `continuous_audio`, `seg["overlay"]`, `_composite_overlay`, `gen_music`/`write_wav`.
- **Invariants I4/I5:** master narration appears exactly once and remains intelligible (music must duck under speech); assembly stays deterministic and AI-free (music must be deterministic/local, NOT a paid generation); graphic text is deterministic (no generative text). Hero baked audio preserved verbatim.
- **Upstream:** graphic beats (S9-C04) carry `graphics` contracts (text/layout). **Downstream:** the final deliverable + `gate_b_review`.
- **Convention:** ffmpeg filtergraph; SHA-bind artifacts.

## Scope
- **Expected change areas:** `build_assembly_manifest` emits (1) a graphic layer per graphic beat (text/layout → deterministic render or overlay PNG) and (2) a music track (path to a deterministic bed). `assemble.py` composites graphics and `amix`es the music bed under the master narration with ducking.
- **Permitted inspection areas:** `generate_music.py` (confirm it is local/deterministic and free — **critical**), `assemble.py` overlay/filtergraph code, `constraints.json` (audio levels, text policy).
- **Protected:** master-narration-once; determinism; the hero temporal-edit guard (D-003); assembly manifest validation.
- **Exclusions:** do not introduce any paid/AI generation in assembly (I5); do not change generation (S9-C06); do not weaken narration intelligibility.
- **Discovery (confirm before implementing):** (a) **Is `generate_music.generate` local and free?** If it is a paid/LLM call, DO NOT use it — instead source a royalty-free bed asset (committed or configured) so assembly stays free/deterministic. (b) How graphic beats' `graphics` contract maps to a render (existing overlay PNG mechanism vs a deterministic text renderer). (c) Ducking approach: simple gain under narration vs sidechain — pick the simplest deterministic option and document the level (e.g., music at -18 to -24 dB under speech).

## Preconditions and baseline
- **Accepted dependencies:** S9-C04 merged (graphic beats present). `ffmpeg`/`ffprobe`.
- **Environment:** `YT_TEST_MODE=1`.
- **Baseline commands:**
  ```bash
  cd /home/jacobw/YTchannel
  git log --oneline -1
  YT_TEST_MODE=1 python3 -m pytest -q
  sed -n '1,60p' scripts/generate_music.py        # confirm local/free
  sed -n '120,183p' scripts/assemble_db.py
  ```
- **Expected current result:** manifest has no graphic/music layer; output has narration only.
- **Blocker conditions:** baseline not green; S9-C04 not merged; `generate_music` is paid (then switch to a provided bed asset or raise BLOCKED).

## Implementation steps
1. **Reproduce (failing test first).** Contract test: `build_assembly_manifest` on a production with a graphic beat → manifest has a graphic layer + a music track; integration test (ffmpeg, short fixture clips) → output audio stream duration matches and a graphic frame is present. Fails today.
2. **Confirm music source is free/deterministic** (Discovery a). If not, use a committed/configured bed asset.
3. **Manifest: graphic layer.** In `build_assembly_manifest`, for each graphic beat emit an overlay/graphic descriptor (reuse the `overlay` mechanism at `assemble.py:253-260`): text/layout from the beat's `graphics` contract, deterministic render path. Non-graphic beats get no overlay.
4. **Manifest: music bed.** Add a `music` key (path to the deterministic bed + target level/ducking params).
5. **Assembler: composite graphics.** Ensure `_composite_overlay` runs for graphic beats (generate/resolve the overlay PNG deterministically; honor text/audio policy + crop safety from `constraints.json`).
6. **Assembler: mix music.** Add an `amix`/`volume` stage mixing the music bed under `continuous_audio` with ducking so narration stays intelligible. Keep master narration exactly once.
7. **Determinism.** Same manifest + clips + bed → same output bytes (seed the music deterministically or use a fixed bed).

## Test and proof matrix
| Level | Scenario | Expected result | Exact command |
|---|---|---|---|
| Contract | manifest has graphic layer per graphic beat + music track | keys present | `YT_TEST_MODE=1 python3 -m pytest tests/test_s9_c07_assembly.py -k manifest_richness -q` |
| Integration | assembled output has narration + music in audio stream | audio stream valid, levels non-trivial | `… -k audio_has_music` |
| Integration | graphic frame present for a graphic beat | frame detectable | `… -k graphic_frame` |
| Unit | master narration appears exactly once (no double) | single narration | `… -k narration_once` |
| Determinism | same inputs → same output bytes | sha stable | `… -k deterministic` |
| Regression | existing assemble tests (D-003/D-005) | green | `YT_TEST_MODE=1 python3 -m pytest tests/test_assemble.py -q` |
| Regression | full suite | green | `YT_TEST_MODE=1 python3 -m pytest -q` |

## Acceptance gates (binary, observable)
- `build_assembly_manifest` emits a graphic layer per graphic beat and a music track.
- The assembled output contains the master narration (exactly once) plus a music component, and a graphic frame for a graphic beat.
- Assembly remains deterministic (same inputs → same bytes) and AI-free (no paid generation).
- Full suite green; no paid call made.

## Audit focus
- Is the music source free/deterministic (no paid/AI in assembly — I5)? Does ducking keep narration intelligible? Is graphic text deterministic (no generative text)? Is master narration still exactly once? Does adding layers break the D-003 hero temporal guard or D-005 manifest validation? Are audio levels policy-compliant (`constraints.json`)?

## Rollback and recovery
- Code + tests only (graphic/music assets committed if needed); `git revert`. Re-assembly is deterministic and idempotent. A bad bed/level is a config/asset swap, not a code change.

## Completion evidence
- Files changed; music-source decision (free/deterministic confirmed) cited; diff summary; commands + exit codes; tests added; observed assembled-output probes (audio levels, graphic frame, sha determinism); residual risks; commit if permitted. Append to `EXECUTION_LOG.jsonl`; update `STATE.json`.
