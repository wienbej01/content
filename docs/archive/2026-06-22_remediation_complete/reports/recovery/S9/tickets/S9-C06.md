# S9-C06 — Real generation request: prompt + hero `--image` + hero `--audio`

## Metadata
- **Ticket ID:** S9-C06
- **Title:** Enrich the Higgsfield generation request with a real per-clip prompt, the hero reference image, and the hero master-narration audio slice; extend the adapter to build `--image`/`--audio`
- **Requirement / risk IDs:** R5 / Q-001 ; risk RK1 (highest)
- **Priority / severity:** P0 / HIGH
- **Risk level:** high (paid, partly-irreversible path)
- **Execution class:** REASONING_CRITICAL
- **Dependencies:** S9-C04 (shot types + visual_intent/prompt source), S9-C05 (slots + hero audio slices + reference linking)
- **Blocks:** the resumption of the real paid run (after this + user `gate_a_spend` re-approval)
- **Wave:** 3
- **Status:** pending

## Observable outcome
For a hero (`HERO_SYNC_LOCKED`, model `seedance_2_0`) render unit, the generation request payload carries (a) a **real per-clip prompt** composed from the beat's visual intent, (b) the **hero reference image** path (`--image`, from the James canonical reference set), and (c) the **hero master-narration slice** path (`--audio`). For b-roll units the payload carries a real prompt only. The `HiggsfieldSeedanceAdapter.submit` constructs `higgsfield generate create seedance_2_0 --prompt <real> --image <ref> --audio <slice> --duration ... --mode fast` for hero and `--prompt <real> --duration ...` for b-roll. **All verified without any real `higgsfield` subprocess call** (subprocess stubbed in tests; a documented `--dry-run`/args-dump verification is run manually before any paid call). Observable by: a contract test asserting the payload fields and the constructed CLI args for hero vs b-roll.

## Evidence and rationale
- **Observed current behavior:** the S9 generation request was `{asset_type, model, duration_ms, audio_policy}`; `prompt` defaulted to `"educational video"`; no `--image`; no `--audio` (so no lip sync).
- **Supporting paths:**
  - `scripts/produce_db.py:541-694` — `invoke_generate_media`; payload built at `:662-667` (minimal); `submit_provider_job(provider="higgsfield", operation="generate_video", request_payload=...)` at `:668-675`.
  - `scripts/paid_adapters.py:32-63` — `HiggsfieldSeedanceAdapter.submit`: `prompt = payload.get("prompt","educational video")` (`:43`); builds `--prompt/--duration/--aspect_ratio/--resolution/--mode` (`:49-57`); **no `--image`/`--audio`**. Registered `:136`.
  - `scripts/produce_db.py:463-479` — `invoke_compile_media` spec merges `visual_intent` (`:478`) and sets `graphic_text_content`; this is the prompt source.
  - `scripts/production_repo.py:367-399` — `plan_render_units` accepts `prompt_revision_id` and `metadata` (store prompt/image/audio refs here).
  - Reference set: `assets/reference/james/canonical/JAMES_*.png` (25 files; e.g. `JAMES_MEDIUM_FRONT_NAVY_SWEATER_SPEAKING_002.png`, `JAMES_THREE_QUARTER_NAVY_SWEATER_002.png`). `configs/james/model_routing.yaml` documents lipsync reference sets (per CLAUDE.md).
  - Lipsync audio slice: produced by S9-C05 (artifact kind `hero_audio_slice`, linked on the unit).
- **Confirmed root cause:** compile never composes/stores a prompt or refs on the unit; generate never reads them; the adapter never builds `--image`/`--audio`.
- **Why necessary:** this is the work that makes the paid test meaningful — real prompts, real lipsync, on-brief b-roll. Without it, paid credits buy generic non-lipsynced clips. (Q-001.)

## Context capsule
- **Paths:** `scripts/produce_db.py` (`invoke_compile_media`, `invoke_generate_media`), `scripts/paid_adapters.py` (`HiggsfieldSeedanceAdapter`), `scripts/production_repo.py` (`plan_render_units`), `configs/james/model_routing.yaml` (reference set, `shot_type_routes`, `model_id_map`, `banned_models`), `assets/reference/james/canonical/`.
- **Key symbols:** `invoke_compile_media`, `invoke_generate_media`, `submit_provider_job`, `HiggsfieldSeedanceAdapter.submit`, `plan_render_units(prompt_revision_id, metadata)`, `shot_type_routes`.
- **Invariants I4/I5:** hero MUST lipsync (seedance_2_0 is the ONLY model with `--audio`); baked audio preserved verbatim at assembly; `seedance_2_0_fast` is in `BANNED_MODELS` (`review_storyboard.py:29`) — use `seedance_2_0` + `--mode fast` per the temporary authorization. Prompt must NOT delegate graphic text to the model (use `graphic_text_content` deterministically — S5/S7 rule).
- **Upstream:** S9-C04 (shot type + visual_intent), S9-C05 (slots + audio slice + ref). **Downstream:** qa_media (validates downloaded clip has audio for HERO_SYNC_LOCKED), assemble (provenance hash from slice).
- **Financial rule I6:** NO real `higgsfield` call in dev/tests. Verify args only.

## Scope
- **Expected change areas:** (1) `invoke_compile_media` composes a per-unit **prompt** (from visual_intent + shot_type + graphic_text_content per constraints) and selects the **hero reference image** (from the configured reference set) and threads the **audio slice ref** (from S9-C05) onto the spec/unit (metadata or prompt_revision). (2) `invoke_generate_media` reads prompt/image/audio_slice into `request_payload`. (3) `HiggsfieldSeedanceAdapter.submit` builds `--image`/`--audio` from the payload when present and the model supports it; adds a **dry-run/args-dump** mode.
- **Permitted inspection areas:** `model_routing.yaml` (reference set, routes), `docs/channel_universe/constraints.json` (negative prompts, text/audio policy, `lipsync_render_rules`), `direct_storyboard.py` prompt conventions, `media_service.submit_provider_job`.
- **Protected:** `FakeProvider` selection in `YT_TEST_MODE` (test path must not hit the real adapter); the D-013 guard; assembly provenance.
- **Exclusions:** do not make any real paid call; do not change qa/assembly logic (S9-C07); do not weaken `BANNED_MODELS`.
- **Discovery (confirm before implementing):** (a) how `model_routing.yaml` names the hero reference set (read the exact key/path); pick a deterministic speaking frame for hero lipsync (not a non-speaking pose). (b) Whether `plan_render_units` persists `metadata`/`prompt_revision_id` such that generate can read them back (verify the SELECT at `produce_db.py:649-657` includes what you store — extend it if needed). (c) The real `higgsfield` CLI flags for `--image`/`--audio` (confirm exact spelling against `higgsfield generate create --help` if available without a paid call; otherwise document the assumed flags and verify in the manual dry-run). (d) Compose-prompt policy: ensure graphic beats use deterministic text, not a generative prompt.

## Preconditions and baseline
- **Accepted dependencies:** S9-C04 + S9-C05 merged. `higgsfield` CLI authenticated (per S9 engineer report) — but **not invoked in tests**.
- **Environment:** `YT_TEST_MODE=1` (so `get_provider_adapter` returns `FakeProvider`, not the real adapter — tests exercise arg-construction via a subprocess stub on the real adapter class directly, NOT through the production generate path which would use FakeProvider).
- **Baseline commands:**
  ```bash
  cd /home/jacobw/YTchannel
  git log --oneline -1
  YT_TEST_MODE=1 python3 -m pytest -q
  sed -n '32,63p' scripts/paid_adapters.py          # current submit
  sed -n '647,676p' scripts/produce_db.py           # current payload
  grep -n "reference\|NAVY\|lipsync" configs/james/model_routing.yaml
  ```
- **Expected current result:** payload lacks prompt/image/audio; adapter omits `--image`/`--audio`.
- **Blocker conditions:** baseline not green; S9-C04/C05 not merged; reference-set config or CLI flags unverifiable (then document assumption + gate behind manual dry-run).

## Implementation steps
1. **Reproduce (failing test first).** Contract test: build a hero render unit (with visual_intent + slice ref from C05 fixtures) and assert the generation payload + adapter CLI args contain a real prompt, `--image <ref>`, `--audio <slice>`; for a b-roll unit assert prompt only, no `--image`/`--audio`. Stub `subprocess.run` so no real CLI fires. Fails today.
2. **Compose prompt (compile).** In `invoke_compile_media`, build `prompt` per unit from `visual_intent` (visual_function/narrative_claim/viewer_takeaway) + `shot_type` + `graphic_text_content`, honoring `constraints.json` negative prompts and text/audio policy. Store on the spec (→ unit `metadata`/`prompt_revision_id`).
3. **Select hero reference (compile).** Read the hero reference set from `model_routing.yaml`; choose a deterministic speaking frame for `hero_lipsync`. Attach its path to hero units only.
4. **Thread audio slice (compile).** Attach the S9-C05 `hero_audio_slice` artifact path/SHA to hero units (already linked by C05 — surface it into generate-readable metadata).
5. **Enrich generate payload.** Extend the SELECT (`produce_db.py:649-657`) and payload (`:662-667`) to include `prompt`, `image_path` (hero), `audio_path` (hero slice), plus `aspect_ratio`/`resolution`/`mode` from routing.
6. **Extend the adapter.** `HiggsfieldSeedanceAdapter.submit`: append `--image` when `payload["image_path"]` present and model is seedance_2_0; append `--audio` when `payload["audio_path"]` present and model supports it (seedance_2_0 only — fail-loud if a non-seedance hero unit requests `--audio`, since that breaks I4). Keep `--prompt` using the real composed prompt.
7. **Add dry-run/args-dump.** Add a mode (env `HIGGSFIELD_DRY_RUN=1` or a payload flag) where `submit` returns the constructed args **without** calling the CLI — so a human can verify the exact request before approving spend. Document the command.
8. **No paid call in tests.** All tests stub `subprocess.run` or use the dry-run mode. Never assert against a real provider response.

## Test and proof matrix
| Level | Scenario | Expected result | Exact command |
|---|---|---|---|
| Contract | hero unit payload has prompt+image+audio | fields present, prompt non-generic | `YT_TEST_MODE=1 python3 -m pytest tests/test_s9_c06_generation.py -k hero_payload -q` |
| Contract | b-roll unit payload has prompt, no image/audio | correct shape | `… -k broll_payload` |
| Contract | adapter builds `--image`/`--audio` for seedance hero (subprocess stubbed) | args contain flags+values, no CLI call | `… -k adapter_hero_args` |
| Contract | adapter omits `--audio` for non-seedance; hero non-seedance fails loud | raises (I4) | `… -k audio_only_seedance` |
| Unit | prompt composed from visual_intent, not "educational video" | prompt reflects intent | `… -k real_prompt` |
| Unit | dry-run mode returns args without subprocess | no real call | `… -k dry_run` |
| Regression | `FakeProvider` still used in YT_TEST_MODE generate path | no real adapter | `YT_TEST_MODE=1 python3 -m pytest tests/ -k generate -q` (adjust) |
| Regression | D-013 TTS guard + full suite | green | `YT_TEST_MODE=1 python3 -m pytest -q` |

## Acceptance gates (binary, observable)
- Hero generation payload = real prompt + image path + audio slice path; b-roll = real prompt only.
- `HiggsfieldSeedanceAdapter.submit` constructs `--prompt/--image/--audio` for a seedance hero and omits `--audio` for non-seedance (failing loud if a hero wrongly lacks seedance), **with zero real `higgsfield` subprocess calls in tests**.
- A documented dry-run command prints the exact hero request (prompt/image/audio/duration/mode) for human inspection.
- Full suite green; **no paid call made**.

## Audit focus (highest scrutiny)
- Is the prompt non-generic and policy-compliant (no graphic text delegated to the model)? Is the reference frame a speaking pose suitable for lipsync? Is the audio slice the correct master subrange (provenance matches C05 + assembly)? Does `--audio` only attach to seedance_2_0 (I4)? Could any test path reach the real adapter / real CLI? Is the dry-run mode actually side-effect-free? Does enriching the payload break `FakeProvider`'s assumptions (it reads `duration_ms`)? Is `seedance_2_0_fast` still banned (not accidentally routed)?

## Rollback and recovery
- Code + tests only; `git revert`. No migration unless a unit column for prompt/image/audio was added (then additive). Re-generation is idempotent (submit is idempotent by key); a malformed request fails before spend. **Before any real paid call**, the human runs the dry-run and re-approves `gate_a_spend`.

## Completion evidence
- Files changed; reference-set source + CLI-flag assumptions cited; diff summary; commands + exit codes; tests added; the dry-run command + sample output; observed contract results; residual risks; commit if permitted. Append to `EXECUTION_LOG.jsonl`; update `STATE.json`. **Flag explicitly: a real paid generation still requires user `gate_a_spend` re-approval — this ticket does NOT authorize it.**
