# ENG-0703

### ENG-0703 — Assembly Timeline Heuristics (Repeated/Looping Assets)

**Owner:** Engineer

**Files:** 

```text
scripts/assemble_db.py
tests/unit/test_assembly_timeline_heuristics.py
```

**Implementation steps:**
1. Add timeline heuristic checks to `validate_assembly_inputs`.
2. Reject timeline if identical `local_graphic` render units repeat consecutively without an explicit editorial break/marker.
3. Reject timeline if a single `local_graphic` remains on screen for an abnormally long duration (e.g., > 15 seconds) unless explicitly marked as a `hold`.
4. Reject timeline if the sequence contains anomalous micro-cuts (e.g., slices < 1 second) suggesting assembly logic breakdown.

**Required tests:**
1. 48 seconds of consecutive identical local graphic spans fails preflight.
2. 10-second valid local graphic passes.
3. Identical consecutive render unit IDs fail preflight.
