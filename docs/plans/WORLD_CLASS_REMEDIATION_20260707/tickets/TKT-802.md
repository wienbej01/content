# TKT-802 — Pre-generation budget allocator

| Field | Value |
|-------|-------|
| Ticket | TKT-802 |
| Title | Pre-generation budget allocator |
| Requirement / risk IDs | R-BUD-2, INV-6 |
| Priority | high |
| Severity | medium |
| Risk level | high |
| Execution class | COMPLEX |
| Wave | 8 |
| Dependencies | W0 |
| Status | planned |

## Observable outcome

`scripts/budget_allocator.py`:
- Computes `total_weight = sum(beat_weights)`.
- Per-beat: `allocation = cap * (beat_weight / total_weight)`.
- Clamped to floor (`MIN_BEAT_BUDGET_USD` = $0.25) and ceiling (`MAX_BEAT_BUDGET_USD` = $8.00).
- Recorded in media plan.
- Total equals cap (within FP tolerance). **No path exceeds cap.**

Uses TKT-006 fixture.

## Context capsule

- Relevant paths: `scripts/budget_allocator.py`, `scripts/beat_weight.py`, `configs/james/model_routing.yaml`.
- Upstream contracts: beat weights from TKT-801.

## Preconditions and baseline

- TKT-801 accepted (beat weights available).
- TKT-006 fixture available.

## Implementation steps

1. Create `scripts/budget_allocator.py` with proportional allocation logic.
2. Use TKT-801 beat weights to compute weighted distribution.
3. Clamp per-beat allocations to floor/ceiling.
4. Verify total = cap within FP tolerance.
5. Record each beat's allocation in the production media plan.
6. Tests in `tests/test_budget_allocator.py`.

## Test and proof matrix

| Level | Scenario | Expected | Command |
|-------|----------|----------|---------|
| allocator | 10-beat fixture, cap $60 | sum = 60 | `python3 -m pytest tests/test_budget_allocator.py::test_sum -q` |
| clamp | very high weight | clamped to ceiling | `python3 -m pytest tests/test_budget_allocator.py::test_clamp_high -q` |
| clamp | very low weight | clamped to floor | `python3 -m pytest tests/test_budget_allocator.py::test_clamp_low -q` |

## Acceptance gates

- G1: Total = cap.
- G2: Per-beat within floor/ceiling.
- G3: Full suite passes.

## Audit focus

Auditor writes independent test: assert `sum(allocations) <= cap` over 1000 random storyboards.
