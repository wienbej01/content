# TKT-601 — Manifest EDL override schema + constraint validation

| Field | Value |
|-------|-------|
| Ticket | TKT-601 |
| Title | Manifest EDL override schema + constraint validation |
| Requirement / risk IDs | R-EDT-1, RISK-5 |
| Priority | high |
| Severity | high |
| Risk level | high |
| Execution class | COMPLEX |
| Wave | 6 |
| Status | planned |

## Observable outcome

`scripts/edl.py` parses YAML/JSON EDL override file with `overrides: [{beat_id, trim_start_sec, trim_end_sec, reorder_after, music_duck_db}]`. Each override validated against the constraint engine (shot-mix bands, hero-block ≤15s). Invalid overrides rejected with named error.

`scripts/assemble.py` applies valid overrides. Behind `EDL_MODE = off|on`. Default `off`.

## Context capsule

- Relevant paths: `scripts/edl.py`, `scripts/assemble.py`, `scripts/review_storyboard.py`, `configs/james/model_routing.yaml`.
- Upstream contracts: constraint engine validates all overrides before assembly re-runs.
- Protected: assembly `off` mode must produce identical output to pre-change code.

## Preconditions and baseline

- Baseline: `python3 scripts/assemble.py <manifest.json> --formats 16x9` (existing assembly flow).
- Record current assembly output to establish regression baseline.

## Implementation steps

1. Create `scripts/edl.py` with parsing logic for YAML/JSON EDL files.
2. Implement constraint validator: check each override preserves shot-mix bands, hero-block ≤15s, trim_min_sec respected.
3. Wire valid overrides into `assemble.py`'s beat loop.
4. Behind `EDL_MODE=off` flag.
5. Tests in `tests/test_edl.py`.

## Test and proof matrix

| Level | Scenario | Expected | Command |
|-------|----------|----------|---------|
| unit | valid EDL override | accepted, assembly applied | `python3 -m pytest tests/test_edl.py::test_valid -q` |
| unit | invalid EDL override (violates bands) | rejected with named error | `python3 -m pytest tests/test_edl.py::test_invalid -q` |
| contract | EDL_MODE=off | no override applied | `python3 -m pytest tests/test_assemble.py -q` |

## Acceptance gates

- G1: Valid override produces correct re-lengths/reorders.
- G2: Invalid override rejected with named error.
- G3: `off` mode leaves existing flow unchanged.
- G4: Full suite passes.

## Audit focus

- Constraint validation: verify the engine correctly rejects any override that would violate invariant RISK-5 (bands preservation).

## Rollback and recovery

- Feature behind `EDL_MODE=on`. Default is `off`. Reverting leaves `EDL_MODE=off`.

## Completion evidence

- Files changed: `scripts/edl.py`, `scripts/assemble.py`.
- New tests: `tests/test_edl.py`.
