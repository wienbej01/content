# S22_T012 - Add human gate_storyboard stage

## Objective

Add a durable human storyboard approval gate after Python validation and Sonnet creative review, before TTS, compile, or media spend.

## Primary agent

Storyboard Architect

## Supporting agents

- DB Feedback Engineer
- Software Engineer
- Software Auditor
- Software Validator

## Recommended model

`deepseek-v4-pro`

## Context files to load first

- `S22_STRUCTURAL_FRAMEWORK.md`
- `S22_PROCESS_RULES.md`
- `scripts/stage_runner.py`
- `scripts/produce_db.py`
- `scripts/authoring_service.py`
- `scripts/production_db.py`
- `tests/e2e/test_s8_projections_resume.py`

## Likely implementation targets

- `scripts/stage_runner.py`
- `scripts/produce_db.py`
- `scripts/authoring_service.py` if needed
- `tests/test_gate_storyboard.py`
- `tests/e2e/test_storyboard_gate_flow.py`
- `reports/karpathy_loop/s22/S22_T012/`

## Explicit non-goals

- Do not change Gate A content semantics.
- Do not remove `gate_a_spend`.
- Do not generate media.
- Do not implement Sonnet generation.

## Required behavior

Add gate behavior:

- `gate_storyboard` depends on storyboard validation and creative review.
- TTS depends on `gate_storyboard`.
- Compile/generate paths cannot bypass unapproved storyboard.
- Approval subject is storyboard revision/hash.
- If storyboard changes, approval becomes stale.
- `YT_TEST_MODE=1` may auto-approve for tests only.

## Detailed tests

Required tests:

1. TTS blocked before `gate_storyboard` approval.
2. Compile blocked before storyboard approval.
3. Approval request subject is active storyboard revision/hash.
4. Storyboard revision change stales approval.
5. `YT_TEST_MODE=1` auto-approves only in test mode.
6. Gate A content still applies to script only.
7. Gate A spend still applies to render plan/cost only.

Required commands:

```bash
python3 -m pytest tests/test_gate_storyboard.py -q
python3 -m pytest tests/e2e/test_storyboard_gate_flow.py -q
python3 -m pytest tests/e2e/test_s8_projections_resume.py -q
```

## Pass gates

- No TTS/compile/generate before storyboard approval.
- Stale storyboard approval cannot be reused.
- Existing approval flow is extended, not duplicated.

## Audit checklist

- Inspect stage registry dependency order.
- Confirm approval staleness works.
- Confirm no bypass route remains.

## Validation checklist

- Run focused and relevant e2e tests.
- Inspect DB approval rows in test evidence.

## Done definition

Done when storyboard approval is a durable stage gate before downstream work.

