# S22_T014 - Update compiler to consume canonical shots

## Objective

Ensure media prompt compilation consumes canonical Sonnet-authored `shots[]` and projected fields, not raw script `visual_brief`.

## Primary agent

Software Engineer

## Supporting agents

- Storyboard Architect
- Software Auditor
- Software Validator

## Recommended model

`deepseek-v4-flash`

## Context files to load first

- `S22_STRUCTURAL_FRAMEWORK.md`
- `scripts/compile_media_prompts.py`
- `scripts/produce_db.py`
- `scripts/shot_router.py`
- `tests/test_compile_media_prompts.py`
- `tests/test_no_legacy_visual_brief_in_production.py` if present

## Likely implementation targets

- `scripts/compile_media_prompts.py`
- `scripts/storyboard_projection.py`
- `tests/test_compile_media_from_canonical_shots.py`
- `tests/test_no_legacy_visual_brief_in_production.py`
- `reports/karpathy_loop/s22/S22_T014/`

## Explicit non-goals

- Do not alter paid generation.
- Do not add overlays.
- Do not implement drift resolution.

## Required behavior

Compiler must:

- Prefer canonical `shots[]`.
- Accept projection fields only if traceable to canonical shot IDs.
- Reject raw script `visual_brief` as production prompt source.
- Preserve negative constraints.
- Preserve text-surface policy.
- Preserve hero reference requirements.
- Include shot IDs in media plan entries.

## Detailed tests

Required tests:

1. Canonical shot compiles into media prompt.
2. Poison script `visual_brief` text does not appear in output.
3. Missing canonical shot ref fails.
4. B-roll prompt contains Sonnet-authored `prompt_intent`/visual concept, not generic fallback.
5. Hero shot still requires reference image.
6. Generated readable text policy still reroutes/blocks.
7. Legacy emergency mode remains explicitly non-production if preserved.

Required commands:

```bash
python3 -m pytest tests/test_compile_media_from_canonical_shots.py tests/test_compile_media_prompts.py -q
python3 -m pytest tests/test_no_legacy_visual_brief_in_production.py -q
```

## Pass gates

- No production prompt path from raw `visual_brief`.
- Media plan entries include canonical shot lineage.
- Existing compiler safety checks still pass.

## Audit checklist

- Search for prompt construction from script `visual_brief`.
- Confirm emergency legacy path is guarded.
- Confirm tests use poison text.

## Validation checklist

- Run focused tests.
- Inspect generated media plan fixture.

## Done definition

Done when media compile is canonical-shot-authoritative.

