profiles:
  sonnet_creative:
    model: kilo/deepseek/deepseek-v4-flash
    purpose: creative/reasoning-heavy tasks
    is_final_creative_authority: true
    default_for: creative
    anti_repetition_directive: "CRITICAL: Never repeat the same phrase, sentence structure, or pattern verbatim. Each sentence must advance the narrative. If you find yourself writing a similar construction to something earlier, restructure it completely."
    max_reroll_attempts: 2

  storyboard_director:
    model: kilo/deepseek/deepseek-v4-flash
    purpose: bulletproof storyboard authoring grounded in all bibles + source text
    is_final_creative_authority: true

  storyboard_director_sonnet5:
    model: kilo/deepseek/deepseek-v4-pro:discounted
    purpose: runtime creative storyboard authoring via DeepSeek V4 Pro through Kilo
    is_final_creative_authority: true
    is_sonnet5_creative_authority: true

  auto_utility:
    model: kilo/deepseek/deepseek-v4-flash
    purpose: mechanical/low-risk tasks
    is_final_creative_authority: false
    default_for: utility

  vision_qa:
    model: kilo/google/gemini-3.5-flash
    purpose: semantic QA of rendered b-roll frames against unit contracts
    is_final_creative_authority: false
    supports_vision: true
    max_tokens: 1024

default_creative: sonnet_creative
default_utility: auto_utility

# TKT-701: Reviewer multi-model cast configuration.
# Each reviewer persona routes through its configured model_profile.
# Default config retains existing behavior (all DeepSeek flash).
# Additional model profiles require human authorization for spending.
reviewer_cast:
  audience: { model_profile: sonnet_creative, weight: 2.0, supports_vision: false }
  filmmaker: { model_profile: sonnet_creative, weight: 1.5, supports_vision: true }
  visual_director: { model_profile: sonnet_creative, weight: 1.5, supports_vision: true }
  brand_voice: { model_profile: sonnet_creative, weight: 1.2, supports_vision: false }
  technical: { model_profile: auto_utility, weight: 0.8, supports_vision: false }

# Tasks that require creative authority (cannot use auto_utility)
creative_authority_tasks:
  - script_review
  - storyboard_generation
  - storyboard_review
  - media_prompt_compilation
  - brand_universe_review
  - audience_retention_review
  - hook_generation
  - framework_building
  - script_writing

# Tasks that require Sonnet 5 creative authority (cannot use any other profile)
storyboard_authority_tasks:
  - storyboard_generation
  - storyboard_repair
  - storyboard_creative_review
