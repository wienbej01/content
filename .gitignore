# Secrets
*.env
runtime.env
~/.config/ytchannel/

# IDE / tools
.kiro/settings/
__pycache__/
*.pyc

# Dependencies
node_modules/

# Generated media (large binaries — not tracked in git)
Videos/
assets/media/
assets/reference/

# Database
db/
!db/
db/*
!db/migrations/
!db/migrations/*.sql

# Temp / raw
content/
.raw_*
/tmp/
ffmpeg2pass*

# Database files (must not be tracked)
*.db
*.sqlite*
!db/migrations/*.sql

# SyncNet clone (external repo — too large for git)
syncnet_python/

# Operational — do not commit
ops/
